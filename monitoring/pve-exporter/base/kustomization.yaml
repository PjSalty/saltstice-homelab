---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - deployment.yaml
  # Secret managed in infrastructure/secrets repo
  - servicemonitor.yaml

labels:
  - pairs:
      app.kubernetes.io/name: pve-exporter
      app.kubernetes.io/component: exporter
