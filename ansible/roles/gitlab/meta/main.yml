---
galaxy_info:
  role_name: gitlab
  author: salty
  license: "Proprietary"
  description: GitLab CE Docker deployment — docker-compose, backup config
  min_ansible_version: "2.17"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies:
  - role: docker
