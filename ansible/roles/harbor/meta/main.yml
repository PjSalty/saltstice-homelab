---
galaxy_info:
  role_name: harbor
  author: salty
  license: "Proprietary"
  description: Harbor container registry — Docker deployment, proxy cache
  min_ansible_version: "2.17"
  platforms:
    - name: Debian
      versions:
        - trixie
dependencies:
  - role: docker
