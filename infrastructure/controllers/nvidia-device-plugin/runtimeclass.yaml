---
# File: kubernetes/infrastructure/controllers/nvidia-device-plugin/runtimeclass.yaml
# Purpose: RuntimeClass for NVIDIA GPU workloads
# Allows pods to use the nvidia container runtime
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
handler: nvidia
