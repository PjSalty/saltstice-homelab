---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-tls-cert
  namespace: monitoring
spec:
  secretName: grafana-tls-cert
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
  dnsNames:
    - grafana.example.com
